# Normalize line endings: store as LF, check out as LF everywhere.
# Prevents the CRLF churn that OneDrive/editors can introduce.
* text=auto eol=lf

# Binary assets — never touch line endings
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
